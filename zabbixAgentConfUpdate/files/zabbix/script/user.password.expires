#zhengning
#20181229 add
UserParameter=user.password.expires[*],/bin/bash /etc/zabbix/script/user.password.expires.sh $1
#用途：用户密码将要过期检查。
#文件：userparameter_zn.common.conf
#传参：number
#  如：
#    user.password.expires[15] :返回将在15天后密码过期的用户列表
#    user.password.expires[0]  :返回密码已过期的用户名列表
